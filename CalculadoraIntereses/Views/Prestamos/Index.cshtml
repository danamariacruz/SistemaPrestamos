@model IEnumerable<CalculadoraIntereses.Models.Prestamos>

@{
    ViewBag.Title = "Prestamos";
}

<h2>Listado de Prestamos</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Plazo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Interes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CantidadPrestada)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdCliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Plazo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Interes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CantidadPrestada)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdCliente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrestamoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrestamoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrestamoID })
        </td>
    </tr>
}

</table>
